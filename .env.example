# Anthropic Claude API Key
# https://console.anthropic.com/ 에서 발급
CLAUDE_API_KEY=sk-ant-api03-...

# Naver Search API
# https://developers.naver.com/ 에서 발급
NAVER_CLIENT_ID=LDuaU_BsVKq7vhG7uYRQ
NAVER_CLIENT_SECRET=y4naSnDw5r

# YouTube Data API v3 (유류/에너지 영상 TOP5)
# https://console.cloud.google.com/ → API 및 서비스 → YouTube Data API v3 활성화 → 사용자 인증 정보 → API 키
YOUTUBE_API_KEY=AIzaSy...

# Supabase (클리핑 히스토리 DB)
# https://supabase.com/dashboard > 프로젝트 선택 > Settings > API 에서 확인
# Project URL: https://xxx.supabase.co
SUPABASE_URL=https://your-project-id.supabase.co
# anon/public 키 (Project API Keys > anon public)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ────────────────────────────────────────────────────────────
# Supabase 테이블 생성 SQL (Supabase Dashboard > SQL Editor 에서 실행)
# ────────────────────────────────────────────────────────────
# CREATE TABLE clips (
#   id          BIGSERIAL PRIMARY KEY,
#   time_slot   INTEGER        NOT NULL,   -- 7 | 11 | 15 | 19
#   clip_date   TEXT           NOT NULL,   -- 'YYYY-MM-DD' (KST 기준)
#   news        JSONB          NOT NULL,   -- 뉴스 배열
#   briefing    TEXT,                      -- AI 브리핑 본문
#   fetched_at  TIMESTAMPTZ    DEFAULT now()
# );
#
# -- 조회 성능용 인덱스
# CREATE INDEX idx_clips_slot_date ON clips (time_slot, clip_date);
#
# -- Row Level Security (anon 읽기/쓰기 허용)
# ALTER TABLE clips ENABLE ROW LEVEL SECURITY;
# CREATE POLICY "anon_read"   ON clips FOR SELECT USING (true);
# CREATE POLICY "anon_insert" ON clips FOR INSERT WITH CHECK (true);
